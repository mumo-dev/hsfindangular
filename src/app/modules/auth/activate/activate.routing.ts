import { Route } from '@angular/router';
import { AuthActivateComponent } from 'app/modules/auth/activate/activate.component';

export const authActivateRoutes: Route[] = [
    {
        path     : '',
        component: AuthActivateComponent
    }
];
